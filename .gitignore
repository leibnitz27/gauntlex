# Built artifact - rebuild from src\ with build\Build-Workbook.ps1
gauntlex.xlsm

# Excel lock / temp files
~$*.xls*
*.tmp

# Loose screenshots / reference images dropped in the repo root
# (move keepers into reference/ to track them)
/*.png
/*.webp

# Editor / OS
.DS_Store
Thumbs.db
